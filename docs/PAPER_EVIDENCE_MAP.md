# Paper Evidence Map

Every quantitative claim in `paper/main.tex`, traced to its source. "Executed" = a real run
produced this number. "Pending" = written into the paper as explicitly not-yet-available.

| Paper claim | Table/Section | Source file | Status |
|---|---|---|---|
| Baseline macro-F1 0.8922 / Weighted-F1 0.9926 | Table I (Stage 3) | `METRICS_HISTORY.md` 2026-09-11 (later) entry; underlying run `proteus/baseline_full.py::run_stage3_classifiers()` -> `results/stage3_classifiers.json` | Executed (prior session) |
| Static augmentation macro-F1 0.8915 | Table I | same as above | Executed (prior session) |
| Drift detector KS stats (0.0027/0.900 stable, 0.329/~0 drifted) | Table II (Stage 4) | `proteus/validate_full.py::run_stage4()` -> `results/stage4_validation.json` | Executed (prior session) |
| Fidelity gate 9/9 noise rejection, 4/9 real admission | Table III (Stage 4) | same as above | Executed (prior session) |
| GAN diversity diagnostic (0/12 mode-collapsed, 3/12 over-dispersed) | Section IV-B, V | `METRICS_HISTORY.md` 2026-09-11 entry | Executed (prior session) |
| BoundedBufferClassifier adaptation characteristics (Table IV) | Table IV (Section VI-D) | `results/stage7_evaluation.json` (protocol-corrected rerun, 2026-09-17, shared eval split across conditions) | Executed. The table's earlier "Full Retraining" comparison column (14.8GB/150s/4,200s/0.1690/0.1484/5.86x) traced to no executed run on disk and has been removed; see `CHANGELOG.md`'s leakage-fix entry for why the one real prior full-retraining run is not comparable. |
| Buffer Capacity Sensitivity Sweep (Small, Default, Large) | Table V (Section VI-D) | `scratch/test_buffer_sensitivity.py` -> `results/buffer_sensitivity.json` | Executed |
| Active-Class Macro-F1 (86.90% active F1 vs 14.71% baseline, final window) | Section VI-D | `proteus/evaluate_full.py::_per_class_breakdown` -> `results/stage7_per_class_f1.json` (2026-09-17; real per-class `classification_report`, no hardcoded active-class count) | Executed. Supersedes an earlier 96.2% figure that cited a nonexistent `scratch/test_per_class_f1.py` and had no backing artifact. |
| GAN/fidelity-gate attribution ablation | Discussion (Section VI-D / Ablation) | `scripts/run_gan_ablation.py` -> `results/gan_ablation.json` (2026-09-17, single-seed diagnostic) | Executed |
| Leakage bug description + fix | Section V, Algorithm 1 | `proteus/closed_loop_full.py::ClosedLoopOrchestrator.split_eval_fit`; verified by `tests/test_closed_loop_leakage.py` | Executed |
| GPU/CUDA claim (RTX 4060, CUDA 12.8, torch 2.11.0+cu128) | Section VI (Experimental Setup) | Direct `torch.cuda.is_available()` check (`STATUS.md`) | Executed |
| Dataset provenance (CICIDS2017 WTMC-2021, InSDN) | Section VI | `data/MANIFEST.md` | Documented, source-hashed |
| Temporal split row counts (1,241,963 / 547,567 / 1,200,000) | Section VI | `METRICS_HISTORY.md` 2026-09-11/2026-09-12 entries | Executed (prior session) |
| All cited references | Bibliography | Standard, well-known papers | Documented |

## Figures

`paper/figures/` contains all three publication figures generated by `paper/generate_figures.py`:
1. `stage3_baseline_vs_static.png` — Stage 3 baseline vs static augmentation comparison.
2. `stage4_fidelity_gate.png` — Stage 4 MMD fidelity gate admission/rejection performance.
3. `stage7_macro_f1_series.png` — Stage 7 temporal drift macro-F1 progression across 16 replay windows (validated post-leakage-fix, incremental `BoundedBufferClassifier`).

All figures generated successfully via `.venv/bin/python paper/generate_figures.py`.
